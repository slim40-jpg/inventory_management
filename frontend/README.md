
  # Stock Management Platform

  This is a code bundle for Stock Management Platform. The original project is available at https://www.figma.com/design/rD7tJS36Ir0buDN7GXshVL/Stock-Management-Platform.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  